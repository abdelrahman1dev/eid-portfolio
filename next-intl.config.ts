const nextIntlConfig = {
  // supported locales
  locales: ["en", "ar"],
  // default locale
  defaultLocale: "en",
  // optional: directory where locale files live (public/locales by default)
  // localeDetection: true // optional
};

export default nextIntlConfig;
